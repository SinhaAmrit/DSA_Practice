//? 02. Write a program to calculate circumference of a circle.
#include <stdio.h>
int main()
{
    int radius = 50;
    printf("Circumference of the circle with radius %d is %d\n", radius, 2 * 22 / 7 * radius);
    return 0;
}