//? 04. Write a program to calculate volume of a cuboid.
#include <stdio.h>
int main()
{
    int length = 12, breath = 4, height = 9;
    printf("Volume of a cuboid having length %d, breath %d & height %d is %d\n", length, breath, height, length * breath * height);
    return 0;
}