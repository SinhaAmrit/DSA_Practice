#include <stdio.h>
int main()
{
    int radius;
    printf("Enter radius of the circle: ");
    scanf("%d", &radius);
    printf("Area of the circle is %d sq units having radius %d units.\n", 22 / 7 * radius * radius, radius);
    return 0;
}