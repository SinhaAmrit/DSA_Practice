//? Write a program to print the first 10 natural numbers.
#include <stdio.h>
int main()
{
    int count = 10;
    for (int i = 1; i <= count; i++)
    {
        printf("%d \n", i);
    }
    return 0;
}